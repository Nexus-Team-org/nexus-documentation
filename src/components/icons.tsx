import { LucideProps } from "lucide-react";

// Re-export all Lucide icons you need
export * from "lucide-react";

// If you need custom icons, you can define them here
// Example:
// export const CustomIcon = (props: LucideProps) => (
//   <svg {...props}>
//     {/* Your SVG path */}
//   </svg>
// );

export const Icons = {
  // Define any custom icons here
  // Example:
  // logo: (props: LucideProps) => (
  //   <svg {...props}>
  //     {/* Your SVG path */}
  //   </svg>
  // ),
};
